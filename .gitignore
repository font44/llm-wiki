.agents/skills/
.env.local
.DS_Store
node_modules/
/tmp/
.qmd/
.direnv/

# Codex project-local CODEX_HOME — only commit config.toml; everything else is runtime state
.codex/*
!.codex/config.toml
!.codex/models.json

# Uncomment if vault grows past ~1 GB:
# raw/pdfs/
# raw/images/
# raw/audio/
